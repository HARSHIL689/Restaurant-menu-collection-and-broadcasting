package org.example.staymate.repo;

import org.example.staymate.model.MenuStore;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MenuStoreRepository extends JpaRepository<MenuStore,String> {

}
